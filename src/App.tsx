import { HashRouter } from "react-router-dom";
import Router from "./components/Router";
import React from "react";

function App() {
  return (
    <HashRouter>
    <Router/>
    </HashRouter>
  );
}

export default App;
